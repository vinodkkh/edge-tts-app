services:
  - type: web
    name: edge-tts-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    plan: free
    autoDeploy: true
    healthCheckPath: /
